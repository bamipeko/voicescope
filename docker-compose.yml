version: '3.8'
services:
  voicescope:
    build: .
    container_name: voicescope
    ports:
      - "5100:5100"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
