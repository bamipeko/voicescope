> これ、他のAIともチェックして進めたいから引き継ぐ方法教えて
> 引継ぎに関しては、ほぼ作り直しを頼むレベルで抜本的に見直したいんだよね。AIの性能も上がってるし、つぎはぎに作ったものを一気に見直すのは価値が大きい。逆に些末な改善点ばかりに注目させるのは避けたい。その前提で指示文作って。
> 特に重視しているのは、スマホ対応が出来ていない点と、ローカルでやろうとすると結構エラーが起きちゃう点。難しいけど、スマホでデータ保存をローカルで出来れば他には負けないアプリになると思うんだよね。

作りました。引き継ぎ用の指示文はここに保存しています。

`Z:\projects\voicescape\state\rebuild_handoff_prompt.md`

このファイルの中身をそのまま Claude / ChatGPT / Gemini / Grok などに貼ればOKです。内容は「小修正ではなく、ほぼ作り直し前提の抜本レビュー」として書いてあります。

渡し方はこうです。

1. 他AIに `state/rebuild_handoff_prompt.md` の全文を貼る
2. リポジトリを読めるAIなら `Z:\projects\voicescape`、見えない場合は `\\192.168.0.92\personal_folder\projects\voicescape` を案内する
3. リポジトリを読めないAIには、最低限 `AGENTS.md`、`STATUS.md`、`state/codex_log.md`、`state/claude_log.md`、`package.json` を添付する
4. 最初は実装させず、総評・技術選定比較・スマホローカル保存設計・ローカルAI設計・移行ロードマップを出させる

指示文には、特にこの方針を強く入れています。

- 既存実装の延命ではなく、勝てる形に再設計する
- スマホ対応を最重要課題として扱う
- スマホで録音・文字起こし・要約データをローカル保存できる設計を重視する
- ローカル処理のエラー多発を、単なるバグではなく設計課題として見る
- Capacitor / React Native / Flutter / PWA / ネイティブを比較させる
- 小さなUI改善や細かいバグ修正リストで終わらせない

今回コード本体は触っていません。追加したのは引き継ぎプロンプトと作業ログだけです。